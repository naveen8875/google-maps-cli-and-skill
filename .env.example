# Copy this to `.env` or export the values in your shell before running proxy-backed jobs.
# Do not commit real proxy credentials.

HTTP_PROXY=http://proxy-host:9000
HTTPS_PROXY=http://proxy-host:9000
ALL_PROXY=
NO_PROXY=localhost,127.0.0.1
